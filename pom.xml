<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.3</version>
        <relativePath/>
    </parent>

    <groupId>com.moreno</groupId>
    <artifactId>electrodomesticos</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    <name>Moreno Electrodomésticos - Gestión de Inventario</name>

    <properties>
        <java.version>21</java.version>
        <javafx.version>21.0.2</javafx.version>
        <itext.version>8.0.3</itext.version>
    </properties>

    <dependencies>
        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- H2 Database (file mode) -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- JavaFX -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- iText 8 PDF -->
        <dependency>
            <groupId>com.itextpdf</groupId>
            <artifactId>kernel</artifactId>
            <version>${itext.version}</version>
        </dependency>
        <dependency>
            <groupId>com.itextpdf</groupId>
            <artifactId>layout</artifactId>
            <version>${itext.version}</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-Xlint:-options</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.moreno.electrodomesticos.MainLauncher</mainClass>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>com.moreno.electrodomesticos.MainLauncher</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- ═══════════════════════════════════════════════════════════════════
         Perfil: windows-installer
         Genera una carpeta autónoma para Windows con JRE embebida.
         Usa jlink para el JRE mínimo y Launch4j para el .exe.
         El cliente NO necesita tener Java instalado.

         Uso:      mvn verify -P windows-installer   (ejecutar en Windows)
         Resultado: target/app/
                      ├── MorenoElectrodomesticos.exe   ← doble clic para abrir
                      ├── electrodomesticos-1.0.0.jar   ← no borrar
                      └── jre/                          ← Java 21 embebido
         Entrega:  comprimir target/app/ en un .zip y enviar al cliente.
         ═══════════════════════════════════════════════════════════════════ -->
    <profiles>
        <profile>
            <id>windows-installer</id>
            <build>
                <plugins>

                    <!-- ── Paso 1 (pre-integration-test): JRE mínimo con jlink ──────── -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>jlink-jre</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>jlink</executable>
                                    <arguments>
                                        <argument>--add-modules</argument>
                                        <!-- Módulos estándar suficientes para Spring Boot + H2 + JavaFX (classpath) + iText -->
                                        <argument>java.base,java.compiler,java.desktop,java.instrument,java.logging,java.management,java.management.rmi,java.naming,java.net.http,java.prefs,java.rmi,java.scripting,java.security.jgss,java.security.sasl,java.sql,java.sql.rowset,java.transaction.xa,java.xml,java.xml.crypto,jdk.management,jdk.unsupported,jdk.zipfs</argument>
                                        <argument>--output</argument>
                                        <argument>${project.build.directory}/app/jre</argument>
                                        <argument>--no-header-files</argument>
                                        <argument>--no-man-pages</argument>
                                        <argument>--strip-debug</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- ── Paso 2 (pre-integration-test): copiar el fat-JAR a target/app ── -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>copiar-jar-a-app</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>run</goal></goals>
                                <configuration>
                                    <target>
                                        <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                              todir="${project.build.directory}/app"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- ── Paso 3 (integration-test): Launch4j genera el .exe con JRE embebida ── -->
                    <plugin>
                        <groupId>com.akathist.maven.plugins.launch4j</groupId>
                        <artifactId>launch4j-maven-plugin</artifactId>
                        <version>2.5.0</version>
                        <executions>
                            <execution>
                                <id>crear-exe-windows</id>
                                <phase>integration-test</phase>
                                <goals><goal>launch4j</goal></goals>
                                <configuration>
                                    <!-- gui = sin ventana de consola negra -->
                                    <headerType>gui</headerType>
                                    <!-- Referencia al JAR ya copiado en target/app/ -->
                                    <jar>${project.build.directory}/app/${project.build.finalName}.jar</jar>
                                    <outfile>${project.build.directory}/app/MorenoElectrodomesticos.exe</outfile>
                                    <errTitle>Moreno Electrodomésticos</errTitle>
                                    <jre>
                                        <!-- Usa ./jre/ junto al .exe (generado por jlink arriba) -->
                                        <path>./jre</path>
                                        <minVersion>21</minVersion>
                                        <runtimeBits>64</runtimeBits>
                                    </jre>
                                    <versionInfo>
                                        <fileVersion>1.0.0.0</fileVersion>
                                        <txtFileVersion>1.0.0</txtFileVersion>
                                        <fileDescription>Gestión de Inventario de Electrodomésticos</fileDescription>
                                        <copyright>Moreno Electrodomésticos</copyright>
                                        <productVersion>1.0.0.0</productVersion>
                                        <txtProductVersion>1.0.0</txtProductVersion>
                                        <productName>Moreno Electrodomésticos</productName>
                                        <internalName>electrodomesticos</internalName>
                                        <originalFilename>MorenoElectrodomesticos.exe</originalFilename>
                                    </versionInfo>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                </plugins>
            </build>
        </profile>
    </profiles>

</project>
